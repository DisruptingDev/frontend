const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function check() { const allEmisores = await prisma.emisors.findMany(); console.log("All emisores count:", allEmisores.length); console.log("Emisores with grupo_id:", allEmisores.map(e => ({ id: e.id, grupo_id: e.grupo_id, rfc: e.rfc, nombre: e.nombre }))); } check().catch(console.error).finally(() => prisma.$disconnect());
