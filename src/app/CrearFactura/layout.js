export const metadata = {
  title: 'Crear Factura',
  description: 'Crea una nueva factura',
}

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  )
}
