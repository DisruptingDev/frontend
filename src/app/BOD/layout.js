export const metadata = {
    title: 'Wise Factura - Alta Usuarios sobre demanda',
    description: 'Alta de usuarios para la plataforma Wise Factura sobre demanda.',
  }
  
  export default function RootLayout({ children }) {
    return (
      <html lang="es">
        <body>{children}</body>
      </html>
    )
  }
  