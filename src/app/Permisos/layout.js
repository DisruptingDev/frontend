export const metadata = {
    title: 'Administración de Permisos',
    description: 'Administración de Permisos en el sistema',
  }
  
  export default function RootLayout({ children }) {
    return (
      <html lang="es">
        <body>{children}</body>
      </html>
    )
  }