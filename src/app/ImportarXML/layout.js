export const metadata = {
    title: 'Importar XML',
    description: 'Importa tus archivos XML',
  }
  
  export default function RootLayout({ children }) {
    return (
      <html lang="es">
        <body>{children}</body>
      </html>
    )
  }