export const metadata = {
    title: 'Validar PPD',
    description: 'Valida tus PPD',
  }
  
  export default function RootLayout({ children }) {
    return (
      <html lang="es">
        <body>{children}</body>
      </html>
    )
  }